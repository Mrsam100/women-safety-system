import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App smoke test placeholder', (tester) async {
    // Placeholder — real tests in test/unit/ and
    // test/integration/ directories.
    expect(true, isTrue);
  });
}
